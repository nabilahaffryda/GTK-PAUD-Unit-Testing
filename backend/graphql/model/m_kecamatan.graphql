type MKecamatan {
    k_kecamatan: Int!
    k_kota: Int!
    singkat: String
    keterangan: String
    kode_dapodik: String
    kode_dagri: Int
    is_aktif: Int!

    m_kota: MKota @belongsTo(relation: "mKota")
}
